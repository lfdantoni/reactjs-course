import React, { Fragment } from 'react';
import styles from './Messages.module.css';

const Messages = () => {
    return (
        <Fragment>
           <h1 className={styles.title}>Titulo</h1> Messages
        </Fragment>
    )
}

export default Messages;
